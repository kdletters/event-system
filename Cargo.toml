[package]
name = "event-system"
version = "0.1.0"
edition = "2024"
authors = ["kdletters"]
repository = ""

[dependencies]
lazy_static = "1.5.0"
lockfree = "0.5.1"

[dev-dependencies]
tokio = { version = "1.46.1", features = ["full"]}